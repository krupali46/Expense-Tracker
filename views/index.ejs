<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <link rel="stylesheet" href="../style.css">
</head>

<body>
    <header>
        <div class="d-flex justify-content-between nav-bg p-4">
            <h2 class="ps-4">Tracker</h2>
            <ul class="d-flex">
                <li class="pe-4"><a href="/">Home</a></li>
                <li><a href="/create">Create</a></li>
            </ul>
        </div>
    </header>

    <h3 class="m-5 text-white text-center">Tracker Entries</h3>
    <div class="container">

        <table class="table table-striped mt-3 text-center m-4">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Type</th>
                    <th scope="col">Category</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Description</th>
                    <th scope="col">Date</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (transactions.length > 0) { %>
                    <% transactions.forEach(function(tracker, index) { %>
                        <tr>
                            <th scope="row"><%= index + 1 %></th>
                            <td><%= tracker.type %></td>
                            <td><%= tracker.category %></td>
                            <td><%= tracker.amount %></td>
                            <td><%= tracker.description %></td>
                            <td><%= new Date(tracker.date).toDateString() %></td>
                            <!-- <td><%= tracker.date.toDateString() %></td> -->
                            <td>
                                <a href="/edit/<%= tracker._id %>" class="btn btn-primary btn-sm">Edit</a>
                                <a href="/delete/<%= tracker._id %>" class="btn btn-danger btn-sm">Delete</a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="7" class="text-center">No tracker entries found.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
